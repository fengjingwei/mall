server:
  port: 9887
  address: 0.0.0.0
  servlet:
    context-path: /nacos-config

spring:
  application:
    name: nacos-config
  profiles:
    active: prod
  cloud:
    nacos:
      config:
        server-addr: 192.168.1.135:8848
        file-extension: yaml
