spring:
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:8000/
      name: gateway
    nacos:
      config:
        server-addr: 192.168.1.135:8848
        group: DEFAULT_GROUP
        data-id: gateway-dynamic-route-dev.json
